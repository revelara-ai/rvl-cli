// Minimal hand-written stub of the `pg` package's public surface, enough for
// the TypeChecker to resolve `pg.Pool` / `pg.Client` from a fixture without a
// heavy real install. See the tsindex README's "fixture stubs" note.
export interface QueryResult {
  rows: any[];
  rowCount: number;
}

export declare class Pool {
  constructor(config?: { connectionString?: string; max?: number });
  query(text: string, values?: any[]): Promise<QueryResult>;
  connect(): Promise<Client>;
  end(): Promise<void>;
}

export declare class Client {
  constructor(config?: { connectionString?: string });
  query(text: string, values?: any[]): Promise<QueryResult>;
  connect(): Promise<void>;
  end(): Promise<void>;
}
