// Minimal hand-written stub of the `typeorm` package: a DataSource class whose
// options carry a nested `extra` bag (where `query_timeout`/`statement_timeout`
// live in real projects). Enough for the TypeChecker to resolve
// `typeorm.DataSource` for the repo_config construction scan.
export interface DataSourceOptions {
  type?: string;
  url?: string;
  maxQueryExecutionTime?: number;
  extra?: {
    query_timeout?: number;
    statement_timeout?: number;
    [key: string]: any;
  };
}

export declare class DataSource {
  constructor(options: DataSourceOptions);
  initialize(): Promise<DataSource>;
  query(sql: string, params?: any[]): Promise<any>;
}
