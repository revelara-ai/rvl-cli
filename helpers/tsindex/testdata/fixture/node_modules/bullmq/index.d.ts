// Minimal hand-written stub of the `bullmq` package's public surface, enough
// for the TypeChecker to resolve `bullmq.Queue` / `bullmq.Worker` from a
// fixture without a heavy real install (same pattern as the pg/axios stubs).
export interface JobsOptions {
  timeout?: number;
  attempts?: number;
  delay?: number;
}

export declare class Queue {
  constructor(name: string, opts?: object);
  add(name: string, data: unknown, opts?: JobsOptions): Promise<unknown>;
  addBulk(jobs: unknown[]): Promise<unknown>;
  close(): Promise<void>;
}

export declare class Worker {
  constructor(
    name: string,
    processor: (job: unknown) => Promise<unknown>,
    opts?: object,
  );
  close(): Promise<void>;
}
