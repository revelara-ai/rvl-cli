// Minimal hand-written stub of winston's public surface, enough for the
// TypeChecker to resolve `winston.Logger` from a fixture without a heavy
// real install. See the tsindex README's "fixture stubs" note.
export declare class Logger {
  debug(msg: string, ...meta: any[]): Logger;
  info(msg: string, ...meta: any[]): Logger;
  warn(msg: string, ...meta: any[]): Logger;
  error(msg: string, ...meta: any[]): Logger;
}
export declare function createLogger(options?: any): Logger;
