// A synchronous fluent SCHEMA BUILDER, standing in for zod/joi/yup and for
// knex's ColumnBuilder. Every method returns the builder itself: no call here
// crosses a process, network or disk boundary, so none of them is a client
// call. Resolving to an external package is NOT sufficient to make a site a
// client site, which is exactly what this fixture pins.
export declare class StringSchema {
  trim(): StringSchema;
  min(n: number): StringSchema;
  uuid(): StringSchema;
  optional(): StringSchema;
  describe(s: string): StringSchema;
}
export declare class SchemaFactory {
  string(): StringSchema;
  object(shape: unknown): StringSchema;
}
declare const z: SchemaFactory;
export default z;
