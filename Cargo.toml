[workspace]
resolver = "2"
members = ["crates/rvlscan"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/revelara-ai/rvlscan"
homepage = "https://revelara.ai"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
